name: Deploy WorkSphere AI

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npm run db:generate

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

  deploy:
    needs: build
    runs-on: ubuntu-latest
    env:
      APP_ENV: ${{ secrets.APP_ENV }}
      REPO: ${{ github.repository }}
    steps:
      - name: Deploy to VPS with SSH_PRIVATE_KEY
        id: deploy_private_key
        continue-on-error: true
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          envs: APP_ENV,REPO
          script: |
            set -e
            APP_DIR="$HOME/work-sphere-ai"
            if [ ! -d "$APP_DIR/.git" ]; then
              git clone "https://github.com/$REPO.git" "$APP_DIR"
            fi
            cd "$APP_DIR"
            git fetch origin main
            git checkout main
            git pull origin main
            if [ -n "$APP_ENV" ]; then
              printf "%s\n" "$APP_ENV" > .env
            fi
            docker compose up -d --build

      - name: Deploy to VPS with SSH_KEY fallback
        if: steps.deploy_private_key.outcome == 'failure'
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          envs: APP_ENV,REPO
          script: |
            set -e
            APP_DIR="$HOME/work-sphere-ai"
            if [ ! -d "$APP_DIR/.git" ]; then
              git clone "https://github.com/$REPO.git" "$APP_DIR"
            fi
            cd "$APP_DIR"
            git fetch origin main
            git checkout main
            git pull origin main
            if [ -n "$APP_ENV" ]; then
              printf "%s\n" "$APP_ENV" > .env
            fi
            docker compose up -d --build
